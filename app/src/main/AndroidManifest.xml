<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET" />
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Doppelkopf"
        tools:targetApi="31">
        <activity
            android:name=".ui.group.JoinGroupActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.group.GroupLoadingActivity" />
        <activity android:name=".ui.group.GroupActivity" />
        <activity android:name=".ui.group.creation.GroupCreationActivity" />
        <activity android:name=".ui.session.SessionLoadingActivity" />
        <activity android:name=".ui.session.SessionActivity" />
        <activity android:name=".ui.session.creation.SessionCreationActivity" />
    </application>
</manifest>